import React from 'react';
import HousesList from '../HousesList/HousesList';
import './Home.scss'

const Home = () => {
    return (
        <div className="home-container">
            <HousesList />

        </div>
    );
};

export default Home;
